package services;

import java.util.TreeSet;

public class BookingServiceImpl implements BookingService{
//Trong class BookingServiceImpl sử dụng TreeSet, static property, static block để lưu trữ và khởi tạo dữ liệu.
// Viết các chức năng xử lý business cho phần hiển thị danh sách booking, thêm mới booking
// (Tạo class Booking Comparator implement Comparator để xử lý phần sắp xếp, override method equals(), hashCode()
// để xử lý phần không trùng lặp).
//    TreeSet<>
}
